[pytest]
markers =
    asyncio: mark test to be run with asyncio
    ai: AI-authored test
    integration: Integration test that requires API access

# To exclude integration tests in CI/CD, set SKIP_INTEGRATION_TESTS=1
# or run: pytest -m "not integration"