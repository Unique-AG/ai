[tool.poetry]
name = "custom-assistant"
version = "0.1.0"
description = ""
authors = ["Konstantin Krauss <konsti@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.0"
unique-sdk = {path = "../..", develop = true}
requests = "^2.32.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "custom_assistant:app.run"