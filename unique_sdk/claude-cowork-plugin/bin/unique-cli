#!/usr/bin/env sh
set -eu

arch="$(uname -m 2>/dev/null || true)"

case "$arch" in
  x86_64|amd64)
    binary="unique-cli-linux-amd64"
    ;;
  aarch64|arm64)
    binary="unique-cli-linux-arm64"
    ;;
  *)
    echo "unique-cli: unsupported architecture: ${arch:-unknown}" >&2
    exit 1
    ;;
esac

dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
target="$dir/$binary"

if [ ! -x "$target" ]; then
  echo "unique-cli: missing executable for $arch: $target" >&2
  echo "Rebuild the plugin with: ./claude-cowork-unique-cli/build/build-plugin.sh" >&2
  exit 1
fi

if [ -z "${UNIQUE_API_KEY:-}" ] && [ -n "${CLAUDE_PLUGIN_OPTION_UNIQUE_API_KEY:-}" ]; then
  export UNIQUE_API_KEY="$CLAUDE_PLUGIN_OPTION_UNIQUE_API_KEY"
fi

if [ -z "${UNIQUE_APP_ID:-}" ] && [ -n "${CLAUDE_PLUGIN_OPTION_UNIQUE_APP_ID:-}" ]; then
  export UNIQUE_APP_ID="$CLAUDE_PLUGIN_OPTION_UNIQUE_APP_ID"
fi

if [ -z "${UNIQUE_USER_ID:-}" ] && [ -n "${CLAUDE_PLUGIN_OPTION_UNIQUE_USER_ID:-}" ]; then
  export UNIQUE_USER_ID="$CLAUDE_PLUGIN_OPTION_UNIQUE_USER_ID"
fi

if [ -z "${UNIQUE_COMPANY_ID:-}" ] && [ -n "${CLAUDE_PLUGIN_OPTION_UNIQUE_COMPANY_ID:-}" ]; then
  export UNIQUE_COMPANY_ID="$CLAUDE_PLUGIN_OPTION_UNIQUE_COMPANY_ID"
fi

if [ -z "${UNIQUE_SKILL_FOLDER:-}" ]; then
  if [ -n "${CLAUDE_PLUGIN_OPTION_SKILLFOLDER:-}" ]; then
    export UNIQUE_SKILL_FOLDER="$CLAUDE_PLUGIN_OPTION_SKILLFOLDER"
  elif [ -n "${CLAUDE_PLUGIN_OPTION_SKILL_FOLDER:-}" ]; then
    export UNIQUE_SKILL_FOLDER="$CLAUDE_PLUGIN_OPTION_SKILL_FOLDER"
  fi
fi

exec "$target" "$@"
