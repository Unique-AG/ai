[pytest]
addopts = --strict-markers --import-mode=importlib
asyncio_mode = auto
markers =
    unit: for unit tests
    serial
    ai: for AI-written tests
filterwarnings =
    ignore:DeprecationWarning