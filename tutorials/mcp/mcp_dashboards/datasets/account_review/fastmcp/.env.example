# Prefer SecretSpec + 1Password (see secretspec.toml). Do not commit real values.
#
#   secretspec check --profile deploy
#   secretspec run --profile deploy --reason "Deploy account-review MCP" -- ./deploy.sh
#
# This file is only a fallback when you are not using secretspec.

# AZURE_SUBSCRIPTION_ID=
# AZURE_RESOURCE_GROUP=rg-lab-demo-001-unique-search-mcp
# AZURE_LOCATION=swedencentral
# AZURE_WEBAPP_NAME=account-review-mcp
# AZURE_CONTAINER_REGISTRY=accountreviewmcpacr
# ZITADEL_BASE_URL=https://id.unique.app
# ZITADEL_CLIENT_ID=
# ZITADEL_CLIENT_SECRET=
# AUTH_DISABLED=true

# Non-secret overrides (safe in shell / CI):
# PUBLIC_BASE_URL=https://account-review-mcp.azurewebsites.net
# PORT=8004
