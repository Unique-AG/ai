# Non-secret URL overrides. Deploy defaults these from the Azure hostname.
# Prefer exporting in the shell; secretspec is not required for these.

# UNIQUE_MCP_PUBLIC_BASE_URL=https://account-review-mcp.azurewebsites.net
# UNIQUE_MCP_LOCAL_BASE_URL=http://127.0.0.1:8004
