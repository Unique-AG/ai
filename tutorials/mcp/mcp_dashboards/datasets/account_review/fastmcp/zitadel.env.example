# Deprecated as a secret store — use secretspec.toml + 1Password instead:
#   secretspec set ZITADEL_BASE_URL --profile deploy
#   secretspec set ZITADEL_CLIENT_ID --profile deploy
#   secretspec set ZITADEL_CLIENT_SECRET --profile deploy
#
# Kept only so deploy.sh can still source a local zitadel.env as a fallback.

# ZITADEL_BASE_URL=https://id.unique.app
# ZITADEL_CLIENT_ID=
# ZITADEL_CLIENT_SECRET=
