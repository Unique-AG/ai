[pytest]
addopts = --strict-markers
asyncio_mode = auto
markers =
    unit: for unit tests
    serial
    ai: AI-authored test
    asyncio: marks tests as async
filterwarnings =
    ignore:DeprecationWarning