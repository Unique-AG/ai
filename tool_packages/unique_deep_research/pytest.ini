[pytest]
addopts = --strict-markers
asyncio_mode = auto
markers =
    asyncio: for async tests
    unit: for unit tests
    ai: AI-authored test
    serial
filterwarnings =
    ignore:DeprecationWarning