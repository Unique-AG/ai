{% if previous_report_items %}
# Current Consolidated Report

Below is the current state of the consolidated report. Each item has an **ID** that you must use to track modifications.

**IMPORTANT: Item IDs are crucial for tracking changes:**
- To **update** an existing item: Keep the same ID and modify its content
- To **add** a new item: Create a new unique ID

{% for item in previous_report_items %}
---

**ID: `{{ item.id }}`**

## {{ item.title }}

{% for bullet in item.bullet_points %}
- **{{ bullet.key_reasoning }}** {{ bullet.detailed_context }}
{% endfor %}
{% endfor %}

---
{% else %}
# Current Consolidated Report

No previous report exists. This is the first batch of insights to consolidate. Create a new consolidated report from the extraction results below.

**When creating new items, assign unique IDs** (e.g., "strength_1", "weakness_1", "opportunity_1", "threat_1").

---
{% endif %}

# New Extraction Results

The following insights have been newly extracted from source documents. Integrate these into the consolidated report.

{% for item in extraction_items %}
### {{ item.title }}

**Justification:** {{ item.justification }}

**References:** {{ item.reference_chunk_ids | join(', ') }}

{% endfor %}
{% if optional_instruction %}
---

# User Instruction

The following instruction has been provided by the user. Please follow it carefully:

{{ optional_instruction }}
{% endif %}
