[pytest]
addopts = --strict-markers
asyncio_mode = auto
markers =
    unit: for unit tests
    serial
    asyncio: for async tests
    ai: AI-authored test
filterwarnings =
    ignore:DeprecationWarning