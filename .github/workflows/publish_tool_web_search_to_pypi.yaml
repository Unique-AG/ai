name: "[unique_web_search 📦] Publish to PyPI"

on:
  workflow_dispatch:
  push:
    # Both conditions must be met (AND logic):
    # 1. Push must be to main branch
    # 2. Push must include changes to web_search/** files
    paths:
      - 'tool_packages/unique_web_search/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.github/workflows/publish_tool_web_search_to_pypi.yaml'
      - '.github/workflows/_reusable_publish.yaml'
    branches:
      - main
    
jobs:
  publish:
    uses: ./.github/workflows/_reusable_publish.yaml
    with:
      package_name: unique-web-search
      package_directory: tool_packages/unique_web_search
    secrets:
      PYPI_TOKEN: ${{ secrets.PYPI_TOKEN }}
    