name: Format Check - unique_mcp

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'unique_mcp/**'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'unique_mcp/**'
  workflow_dispatch:

jobs:
  format-check:
    uses: ./.github/workflows/ruff-format-check.yml
    with:
      directory: unique_mcp
      python-version: '3.12'

