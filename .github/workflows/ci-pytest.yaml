name: "[CI] Pytest - All Packages"

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to test (leave empty for auto-detect based on changes)'
        type: choice
        options:
          - ''
          - all
          - toolkit
          - sdk
          - mcp
          - orchestrator
          - web_search
          - quartr
        default: ''

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            toolkit:
              - 'unique_toolkit/**'
              - '!unique_toolkit/docs/**'
            sdk:
              - 'unique_sdk/**'
            mcp:
              - 'unique_mcp/**'
            orchestrator:
              - 'unique_orchestrator/**'
            web_search:
              - 'tool_packages/unique_web_search/**'
            quartr:
              - 'connectors/unique_quartr/**'

      - name: Select packages
        id: set-matrix
        uses: ./.github/actions/select-packages
        with:
          packages: |
            [
              {"id": "toolkit",            "dir": "unique_toolkit",                            "python": "3.12", "install_args": "--with fastapi"},
              {"id": "sdk",                "dir": "unique_sdk",                                "python": "3.11", "test_args": "-m \"not integration\""},
              {"id": "mcp",                "dir": "unique_mcp",                                "python": "3.12"},
              {"id": "orchestrator",       "dir": "unique_orchestrator",                       "python": "3.12"},
              {"id": "web_search",         "dir": "tool_packages/unique_web_search",           "python": "3.12"},
              {"id": "quartr",             "dir": "connectors/unique_quartr",                  "python": "3.12"}
            ]
          selected: ${{ github.event.inputs.package }}
          changes: ${{ toJson(steps.filter.outputs) }}

  test:
    needs: detect-changes
    if: ${{ fromJson(needs.detect-changes.outputs.matrix).include[0] != null }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    name: test-${{ matrix.id }}
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        uses: ./.github/actions/run-tests
        with:
          package_dir: ${{ matrix.dir }}
          python_version: ${{ matrix.python }}
          install_args: ${{ matrix.install_args }}
          test_args: ${{ matrix.test_args }}
