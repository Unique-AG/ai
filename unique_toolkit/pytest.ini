[pytest]
addopts = --strict-markers
asyncio_mode = auto
markers =
    unit: for unit tests
    serial
    ai_generated: for tests generated by AI
    ai: for AI-authored tests
filterwarnings =
    ignore:DeprecationWarning