[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    ai_generated: Tests generated or modified with AI assistance
    integration: Integration tests (slower, test multiple components)
    unit: Unit tests (fast, test single functions)
addopts = 
    -v
    --strict-markers
    --tb=short

