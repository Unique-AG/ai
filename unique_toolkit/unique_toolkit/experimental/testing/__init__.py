"""Fixtures and helpers for tests that consume the toolkit.

Renamed from ``test_utilities`` to follow the conventional Python name and
surfaced at the top level so downstream repos can import
``from unique_toolkit.testing import …``.

Planned contents (current home → new home):

* all modules under :mod:`unique_toolkit.test_utilities`
"""
